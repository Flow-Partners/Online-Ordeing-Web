# Force npm to use a specific registry
registry=https://registry.npmjs.org/

# Set save-exact to true to ensure exact versions are saved
save-exact=true

# Increase the timeout for slower networks
fetch-timeout=60000

# Enable package-lock.json
package-lock=true

